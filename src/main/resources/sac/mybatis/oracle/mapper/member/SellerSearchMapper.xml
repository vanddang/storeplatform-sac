<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SellerSearch">



    <!-- 탈퇴 사유 목록 조회 -->
    <select id="listWithdrawalReason" parameterType="ListWithdrawalReasonReq" resultType="SecedeReson">
        SELECT /* SellerSearchMapper.xml, listWithdrawalReason, SAC : 한서구 , 2014-01-08 */
                cd_id AS secedeReasonCode
              , cd_nm AS secedeReasonMessage
          FROM 
                tb_cm_cd
         WHERE  grp_cd_id = 'US0104'
           AND  lang_cd =#{koUsWhether} 
    </select>
    
    <!-- 나라별 해외은행 정보 조회 -->
    <select id="listBanksByCountry" parameterType="ListBanksByCountryReq" resultType="BanksByCountry">
        SELECT /* SellerSearchMapper.xml, listBanksByCountry, SAC : 한서구 , 2014-01-08 */
		        nation_cd     AS nationCode
		      , nation_nm     AS nationName
		      , alpha2_cd     AS alphaCode
		      , bank_no       AS bankNo
		      , nation_tel_no AS nationTelNo
		      , nation_desc   AS nationDesc
              , iban_use_yn   AS ibanUseYn
              , aba_use_yn    AS abaUseYn
              , sort_use_yn   AS sortUseYn
              , bsb_use_yn    AS bsbUseYn
		  FROM 
		        tb_cm_nation
		 WHERE  use_yn=#{useYn} 
    </select>
    
</mapper>
